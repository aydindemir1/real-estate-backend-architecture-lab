server:
  port: 8888

spring:
  application:
    name: config-server-local
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config-repo
